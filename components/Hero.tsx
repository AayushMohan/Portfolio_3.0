import React from "react";
import BackgroundCircles from "./BackgroundCircles";

type Props = {};

const Hero = (props: Props) => {
  return (
    <div>
      <BackgroundCircles />
    </div>
  );
};

export default Hero;
